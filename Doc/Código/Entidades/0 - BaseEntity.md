Entidade base do projeto, serve apenas como classe pai na herança para evitar repetição de campos base do banco.
# Campos
- Id (Guid)
- CreatedAt (DateTime)
- UpdatedAt (DateTime)
# Regras
- Não possui nenhuma regra atrelada. 
